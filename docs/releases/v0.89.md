# OpenLadder Studio v0.89

## Laboratorio PG TP02

- corrige o comportamento observado no ensaio fisico da v0.88 em que o perfil validado `19200 8O1`, `DTR=on`, `RTS=on` transmitiu oito HELLOs e recebeu zero bytes em todas as tentativas;
- o motor PG Lab passa para 1.4;
- apos cada duas falhas consecutivas do `CON-ICB<CR>`, o laboratorio rearma DTR/RTS sem transmitir nenhum quadro adicional;
- apos cada quatro falhas consecutivas, fecha e reabre a mesma porta serial e restaura o perfil validado;
- o numero de tentativas do HELLO sobe de 8 para 10;
- o estado de validacao do F0 continua sendo apagado sempre que a sessao e rearmada;
- a sequencia segura permanece `HELLO -> F0 00 0F -> 38 00 C7`;
- `38 00 C7` continua exigindo permissao manual READ-ONLY, allowlist explicita e resposta F0 `00 02 10 22 CB` na mesma sessao;
- nenhum comando de escrita, RUN/STOP remoto, download, apagamento ou firmware foi habilitado.
