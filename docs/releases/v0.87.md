# OpenLadder Studio 0.87

## TP02 PG — validação física do preflight

- validado em bancada o perfil `19200 8O1`, com DTR e RTS ligados;
- `CON-ICB<CR>` recebeu `80 01 09 75` em aproximadamente 248 ms na sessão observada com o PLC em STOP;
- `F0 00 0F` recebeu `00 02 10 22 CB` em aproximadamente 267 ms;
- os dois quadros binários observados fecham a soma módulo 256 em `FF`;
- a resposta ao F0 confirma fisicamente o comprimento previsto de 5 bytes (`CMD=00`, `LEN=02`, payload `10 22`, checksum `CB`);
- a escuta passiva por 3 s após o F0 não recebeu bytes adicionais;
- `TP02-PG-Tests.json` passa a reconhecer essa resposta como vetor esperado;
- `38 00 C7` continua desabilitado e classificado como candidato; nenhum comando novo de escrita foi liberado.

A semântica dos bytes de payload `10 22` ainda não foi atribuída. O próximo ensaio deve comparar estados RUN/STOP e testar a sequência de leitura apenas dentro da mesma sessão validada.