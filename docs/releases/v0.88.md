# OpenLadder Studio v0.88

## Laboratorio PG TP02

- prepara o primeiro ensaio controlado de `38 00 C7` dentro da mesma sessao que confirmou o handshake e o preflight `F0 00 0F`;
- o pacote usa somente o perfil fisicamente validado: `19200 8O1`, `DTR=on`, `RTS=on`;
- `F0 00 0F` precisa retornar o vetor confirmado `00 02 10 22 CB` antes que o candidato seja elegivel;
- `38 00 C7` passa a usar a classe `READ_ONLY_CANDIDATE`, distinta de comandos verificados e de candidatos meramente documentados;
- o quadro candidato exige autorizacao manual de READ-ONLY, presenca explicita na allowlist, preflight F0 validado na mesma porta aberta e correspondencia exata com `38 00 C7`;
- o motor PG Lab passa para 1.3 e zera a prova do F0 a cada nova sessao serial;
- a resposta ao `38` e capturada sem formato presumido, com busca por subquadros cuja soma modulo 256 fecha em `FF` e mais 5 s de escuta passiva se houver resposta.

## Seguranca

Nenhum outro candidato foi liberado. `34 03 00 00 A0 28`, quadros `0A`, `14 00 EB`, RUN/STOP remoto, escrita e download continuam desabilitados. `0F 00 F0` continua bloqueado internamente por estar associado a Clear All Memory.
