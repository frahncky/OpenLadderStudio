# Notas de implementação

A revisão visual deve preservar o projeto WinForms e a compatibilidade com Windows 7, evitando dependências externas. Preferir desenho próprio com `System.Drawing`, `Panel`, `Button`, `ToolStrip`, `TabControl` e controles nativos estilizados.
