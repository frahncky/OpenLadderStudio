name: Capturar handshake PG do PC12

on:
  workflow_dispatch:

jobs:
  capture:
    runs-on: windows-latest
    steps:
      - name: Observação
        shell: pwsh
        run: Write-Host 'Instrumentação dinâmica desativada no runner hospedado; VirtualAllocEx é bloqueado pela política do ambiente.'
